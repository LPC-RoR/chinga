<%= form_with(model: registro, local: true) do |form| %>
  <% if registro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registro.errors.count, "error") %> prohibited this registro from being saved:</h2>

      <ul>
      <% registro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :interprete_id %>
    <%= form.number_field :interprete_id %>
  </div>

  <div class="field">
    <%= form.label :elemento_id %>
    <%= form.number_field :elemento_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
