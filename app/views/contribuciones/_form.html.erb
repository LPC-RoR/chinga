<%= form_with(model: contribucion, local: true) do |form| %>
  <% if contribucion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contribucion.errors.count, "error") %> prohibited this contribucion from being saved:</h2>

      <ul>
      <% contribucion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
