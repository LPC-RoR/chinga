<%= form_with(model: texto, local: true) do |form| %>
  <% if texto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(texto.errors.count, "error") %> prohibited this texto from being saved:</h2>

      <ul>
      <% texto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :texto %>
    <%= form.text_field :texto %>
  </div>

  <div class="field">
    <%= form.label :sha1 %>
    <%= form.text_field :sha1 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
