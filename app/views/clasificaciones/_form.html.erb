<%= form_with(model: clasificacion, local: true) do |form| %>
  <% if clasificacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clasificacion.errors.count, "error") %> prohibited this clasificacion from being saved:</h2>

      <ul>
      <% clasificacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :carpeta_id %>
    <%= form.number_field :carpeta_id %>
  </div>

  <div class="field">
    <%= form.label :elemento_id %>
    <%= form.number_field :elemento_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
