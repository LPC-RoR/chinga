<%= form_with(model: observacion, local: true) do |form| %>
  <% if observacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(observacion.errors.count, "error") %> prohibited this observacion from being saved:</h2>

      <ul>
      <% observacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :observacion %>
    <%= form.text_field :observacion %>
  </div>

  <div class="field">
    <%= form.label :detalle %>
    <%= form.text_area :detalle %>
  </div>

  <div class="field">
    <%= form.label :elemento_id %>
    <%= form.number_field :elemento_id %>
  </div>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.number_field :owner_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
