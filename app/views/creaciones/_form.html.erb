<%= form_with(model: creacion, local: true) do |form| %>
  <% if creacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(creacion.errors.count, "error") %> prohibited this creacion from being saved:</h2>

      <ul>
      <% creacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :autor_id %>
    <%= form.number_field :autor_id %>
  </div>

  <div class="field">
    <%= form.label :elemento_id %>
    <%= form.number_field :elemento_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
